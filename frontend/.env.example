# Point the app at your Collaberry gateway (Envoy). Copy to .env for a device
# build, or export these inline. Defaults (localhost:8080) live in app.json.
EXPO_PUBLIC_API_URL=http://localhost:8080
EXPO_PUBLIC_WS_URL=ws://localhost:8080
