# Expo's dependency graph has intentional peer-range overlaps across the RN
# ecosystem; legacy-peer-deps lets a plain `npm install` resolve them the same way
# the Expo CLI does. Pin the official registry so a stale global mirror can't break
# installs on this machine.
legacy-peer-deps=true
registry=https://registry.npmjs.org/
